class Relu:
    def forward(self, x, y):
        if x>0:
            y=x
        else:
            y=0
        return y
    
    def backward(self,dout):
        if self.x>0:
            dx = dout
        else:
            dx = 0

class Affine:
    def forward(self,x,w,y):
